Folder fro the implementation of the learned beased deviaitions.

Steps that will be done: 

- Neural network on a small image (32x32) see how well it performs (actually NN.py)
- NN on only the primal variables, compare to the results of the previous case 
- Try to scale up the problem with what has been learned on a small image
